const initialiseFrontbase = async (db) => {
  console.log(
    'Hi! Welcome to FrontBase. Give us a second to set-up the server :)'
  )

  console.log('--> Server setup: TODO')

  return 'setup'
}
export default initialiseFrontbase
